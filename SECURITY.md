# Security policy

Do not disclose credentials, updater signing material, private documents or
unredacted logs in a public issue. Report security vulnerabilities privately
through GitHub Security Advisories for `jedi-hex/odoc`.

Updater signing keys are never stored in this repository. The public
verification key is embedded in the application; the private key is held by
the release environment and backed up offline.
